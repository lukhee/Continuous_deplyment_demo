---
  - name: Exercise for setting up a web server in an ubuntu Server EC 
    hosts: all
    user: ubuntu
    become: yes
    become_method: sudo
    become_user: root
    roles:
    - prepare
    - hostServer


#! CLI to fetch ip-address from aws
#  aws ec2 describe-instances --query 'Reservations[*].Instances[*].PublicIpAddress' --filter "Name=tag:Name,Values=ansible_test" --output text
---
  - name: Exercise for setting up a web server in an ubuntu Server EC 
    hosts: all
    user: ubuntu
    become: yes
    become_method: sudo
    become_user: root
    roles:
      - prepare
      - hostServer
